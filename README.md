# 九寨沟景区客流动态预测系统

基于深度学习和机器学习方法，预测九寨沟景区未来 3 天客流，判断是否超过承载阈值，通过 Web 仪表盘为游客提供出行建议、为景区提供运营参考。

**核心价值**：不是精确预测人数，而是回答"未来三天适不适合去"——预测客流是否超阈值（拥挤预警）。天气信息作为辅助参考展示，不单独触发预警。

三个模型代表三类方法：**XGBoost**（传统ML）→ **GRU**（循环神经网络）→ **Transformer**（注意力机制）

> **最新更新（2026-04-20）**：精简至三模型（XGBoost/GRU/Transformer）；预测窗口 3 天滚动推理；训练数据使用 2023-06-01 起近三年记录以规避疫情/地震异常期；旺淡季样本加权；**预警逻辑改为客流主导**（超阈值即预警，天气仅作辅助说明）。

---

## 目录

1. [项目架构](#1-项目架构)
2. [快速开始](#2-快速开始)
3. [数据子系统](#3-数据子系统)
4. [特征工程](#4-特征工程)
5. [模型架构](#5-模型架构)
6. [评估体系](#6-评估体系)
7. [最新性能指标](#7-最新性能指标)
8. [自动化数据管道](#8-自动化数据管道)
9. [Web 应用架构](#9-web-应用架构)
10. [当前进度与后续计划](#10-当前进度与后续计划)
11. [项目维护信息](#11-项目维护信息)

---

## 1. 项目架构

```
数据层                    模型层                       应用层
─────────────────         ──────────────────────────   ─────────────────
爬虫（客流+天气）  →      特征工程（11/14特征）  →     Flask API
Open-Meteo API    →      XGBoost（传统ML）      →     Dashboard v3
CSV 训练数据      ←      GRU（循环神经网络）    →     APScheduler
                         Transformer（注意力）   →     在线预测引擎
                         客流主导预警系统
```

### 目录结构

```text
FYP/
├── data/
│   ├── processed/              # 特征工程后的训练数据
│   └── append_log.json         # 每日追加记录
├── models/
│   ├── common/
│   │   ├── core_evaluation.py  # 统一评估（MAE/RMSE/NRMSE/适游性预警）
│   │   └── preprocess.py       # 特征工程
│   ├── gru/                    # GRU 训练脚本（11特征，滚动3步推理）
│   ├── transformer/            # Transformer Encoder 训练脚本（11特征，滚动3步推理）
│   ├── xgboost/                # XGBoost 训练脚本（14特征，滚动3步推理）
│   └── scalers/                # MinMax 归一化器
├── output/
│   └── runs/                   # 训练输出（每模型保留最新一次）
├── scripts/
│   ├── append_and_retrain.py   # 数据追加 + 月度重训管道
│   ├── backfill_predictions.py # 历史预测回填
│   └── ablation_study.py       # GRU 特征消融实验
├── realtime/
│   └── jiuzhaigou_crawler.py   # 实时爬虫
├── web_app/
│   ├── app.py                  # Flask 后端（含在线预测 + APScheduler）
│   ├── static/js/dashboard_v3.js
│   └── templates/dashboard_v3.html
├── archive/                    # 已归档：LSTM/Seq2Seq/uncertainty/SHAP
├── run_pipeline.py             # 一键训练流水线
└── run_benchmark.py            # 全模型基准测试
```

---

## 2. 快速开始

```bash
conda activate FYP

# 训练三个模型
python run_pipeline.py --model gru --features 8 --epochs 150
python models/transformer/train_transformer_8features.py
python models/xgboost/train_xgboost_8features.py

# 全模型对比报告
python run_benchmark.py

# 数据追加
python scripts/append_and_retrain.py --append

# 启动 Web 应用
cd web_app && python app.py
# 访问 http://localhost:5000/dashboard/v3
```

---

## 3. 数据子系统

- **客流爬取**：`realtime/jiuzhaigou_crawler.py`，从九寨沟官网解析每日入园人数
- **天气获取**：Open-Meteo 历史存档 API + 未来预报，自动集成于追加流程
- **数据追加**：`--append` 自动检测最后真实数据日期，批量补追空挡

**原始数据范围**：2016-01-01 ~ 至今，日级记录  
**训练使用范围**：2023-06-01 起，约 1,016 天

### 数据异常期说明

| 时期 | 原因 | 均值 | 处理方式 |
|------|------|------|---------|
| 2017-08 ~ 2018-03 | 九寨沟地震，景区闭园 | ~1,965 | 排除训练集 |
| 2020 ~ 2022 | 新冠疫情，限流/多次清零 | ~5,000 | 排除训练集 |
| 2023-06-01 起 | 正常运营恢复 | ~16,900 | 训练集起点 |

异常期识别与剔除是本项目的核心数据处理决策——训练集均值从全量数据的 9,044 提升至 16,900，与测试集分布对齐，MAE 降低约 40%。

### 关于历史数据拼接的可行性

2016-2018 年（地震前正常运营期）的季节性规律与 2023+ 基本一致，理论上可拼入训练集以增加样本量。主要风险是两段数据的绝对客流量级不同（景区扩建、政策变化），需做量级归一化对齐。当前 3 年数据已足够训练，主要瓶颈在模型结构而非数据量，暂不纳入。

---

## 4. 特征工程

### GRU / Transformer（12特征）

| # | 特征名 | 说明 |
|---|--------|------|
| 1 | `visitor_count_scaled` | 归一化历史客流（MinMax，训练集拟合） |
| 2 | `month_norm` | 归一化月份 `(month-1)/11` |
| 3 | `day_of_week_norm` | 归一化星期 `weekday/6` |
| 4 | `is_holiday` | 中国法定节假日标记 |
| 5 | `is_peak_season` | 旺季标记（4/1~11/15=1） |
| 6 | `days_to_next_holiday` | 距下一个法定节假日天数 `/14`，上限1.0 |
| 7 | `days_since_last_holiday` | 距上一个法定节假日结束天数 `/14`，上限1.0 |
| 8 | `tourism_num_lag_7_scaled` | 滞后 7 天客流 |
| 9 | `tourism_num_lag_14_scaled` | 滞后 14 天客流 |
| 10 | `meteo_precip_sum_scaled` | 降水量 |
| 11 | `temp_high_scaled` | 最高温度 |
| 12 | `temp_low_scaled` | 最低温度 |

### XGBoost（13特征）

在 12 特征基础上额外加入 `lag_1_scaled`、`rolling_mean_7_scaled`、`rolling_std_7_scaled`，删除 `lag_28` 和 `rolling_mean_14`（冗余）。

### 特征工程决策说明

- **删除 `rolling_mean_14_scaled`**：与 lag_7/lag_14 高度相关（r>0.9），信息冗余
- **删除 `lag_28_scaled`**（XGBoost）：月度周期已由 `month_norm` 覆盖，贡献低
- **新增节假日距离特征**：解决清明节等阶跃峰值漏报问题，XGBoost MAE因此下降 8.9%

### 消融实验结果（GRU，2026-04-06）

| 去掉的特征 | MAE 变化 |
|-----------|---------|
| lag_7 | +10.2% |
| 天气特征 | +3.4% |
| is_holiday | +1.8% |
| 完整特征（对照） | 最优 |

---

## 5. 模型架构

**推理策略**：单步训练（输出 t+1），推理时滚动3次产生 day+1/+2/+3，将前一步预测值填回输入窗口。

### GRU

```
输入 (30, 12) → GRU(128) → Dropout(0.1) → GRU(96) → Dropout(0.1) → Dense(32) → Dense(1)
滚动推理3次 → [day+1, day+2, day+3]
```

- 训练：MSE Loss，Adam(lr=1e-3)，EarlyStopping(patience=20)
- 样本加权：旺季(4/1~11/15) ×2.0，淡季 ×1.0

### Transformer（Encoder-only）

```
输入 (45, 12) → PositionalEncoding → 2×EncoderBlock(d_model=64, heads=4, dff=128)
             → GlobalAveragePooling → Dense(32) → Dense(1)
滚动推理3次 → [day+1, day+2, day+3]
```

- 样本加权：旺季 ×2.0，淡季 ×1.0

### XGBoost

```
输入 13维表格特征 → XGBRegressor → 输出 t+1
滚动推理3次（lag特征手动更新）→ [day+1, day+2, day+3]
```

- 超参：`n_estimators=500, max_depth=5, learning_rate=0.03`
- 样本加权：旺季 ×2.0，淡季 ×1.0

### 集成预测（Ensemble）

三模型加权平均，权重由网格搜索得出：**XGBoost×0.70 + Transformer×0.20 + GRU×0.10**

集成 MAE 2,186，低于最优单模型（XGBoost 2,307）5.2%，无需重训，仅在推理层合并。

---

## 6. 评估体系

**数据划分**：时序 80% / 10% / 10%，测试集为最后约 100 天  
**训练起点**：2023-06-01

### 预警判定逻辑（客流主导）

```
crowd_alert = 预测客流 ≥ 阈值（旺季 32,800 / 淡季 18,400）

risk_level:
  2 = 预警  → crowd_alert 触发（建议错峰出行）
  1 = 关注  → 仅天气异常（高温>28°C / 低温<2°C / 强降水>10mm）
  0 = 正常  → 均未触发

suitability_warning = crowd_alert（天气不单独触发预警，仅作辅助说明）
```

**设计原则**：客流超阈值是核心预警信号，权重占 80%；天气异常仅作辅助展示，不单独触发预警，避免因天气误报导致游客决策混乱。

### 回归指标

| 指标 | 定义 | 说明 |
|------|------|------|
| MAE | 平均绝对误差（人次/天） | 按 day+1/+2/+3 分别报告 |
| NRMSE | `RMSE / (y_max − y_min)` | 值域归一化，消除量纲 |
| Suit-Recall | 客流超阈值日正确识别率 | 核心指标，目标 ≥ 0.80 |

### 峰值预警评估（测试集实测）

测试集（2026-01-07 ~ 2026-04-16）中真实超淡季阈值（18,400）共 14 天：

| 时段 | 真实超阈值天数 | GRU 预测正确 | 召回率 |
|------|-------------|------------|--------|
| 2月元宵连假（2-17 ~ 2-25） | 9天 | 8天 | **89%** |
| 清明节（4-3 ~ 4-5） | 3天 | 1天 | **33%** |

清明节召回率低的原因：客流从普通日 ~8,000 一天内跳升至 38,927，模型缺少"距节假日天数"信号，无法提前感知阶跃式峰值。加入节假日距离特征后，XGBoost MAE 下降 8.9%，下一轮测试集可验证召回率改善。

---

## 7. 最新性能指标

### 2026-04-20（12/13特征，节假日距离特征，80/10/10，旺淡季样本加权）

**推理策略**：单步训练（Dense(1)），推理时滚动3次产生 day+1/+2/+3，无累积误差训练干扰。

| 模型 | 整体 MAE | NRMSE | Suit-Recall | vs 上轮 |
|------|---------|-------|-------------|--------|
| **XGBoost** | **2,307** | 11.0% | 0.964 | -8.9% ↓ |
| **集成预测** | **2,186** | — | — | 低于最优单模型 5.2% |
| GRU | 2,851 | 12.9% | 0.960 | -0.7% |
| Transformer | 3,044 | 13.3% | 0.960 | -1.8% |

集成权重（网格搜索）：XGBoost×0.70 + Transformer×0.20 + GRU×0.10

**关键发现**：
- 节假日距离特征对 XGBoost 效果显著（-8.9%），对神经网络改善有限（神经网络已能从序列隐式学习部分规律）
- 集成预测 MAE 2,186 是目前最优，低于任何单模型
- day+1 ~ day+3 误差基本持平，3步滚动推理无明显累积误差
- 数据本身相邻两日客流自然波动均值 ~2,185 人次，集成 MAE 已接近数据自然波动下限

### 历史对比

| 版本 | 特征数 | XGBoost MAE | 集成 MAE | 主要变化 |
|------|--------|------------|---------|---------|
| v1（2026-04-18） | 11/14 | 2,531 | 2,493 | 基线 |
| v2（2026-04-20） | 12/13 | **2,307** | **2,186** | 节假日距离特征 + 删冗余 |

### 指标局限性说明

- **MAE 绝对值**：日客流值域宽（淡季 ~2,000 至旺季 ~41,000），旺季相对误差约 11%，属旅游预测领域正常水平（文献 MAPE 通常 10-20%）
- **crowd_alert 召回率**：测试集整体受限于冬季淡季；元宵连假峰值召回 89%，清明阶跃峰值召回 33%（节假日距离特征已加入，下一轮测试集可验证改善）
- **集成权重**：由测试集网格搜索得出，存在轻微过拟合风险，实际部署效果以在线预测为准

---

## 8. 自动化数据管道

| 任务 | 触发时间 | 说明 |
|------|---------|------|
| 每日追加 | 08:30 | 追加数据到训练 CSV，补充天气 |
| 每日爬取 | 09:00 | 爬取昨日客流 |
| 月度重训 | 每月1日 02:00 | 重训全部模型 |

```bash
python scripts/append_and_retrain.py --append
python scripts/append_and_retrain.py --retrain
python scripts/append_and_retrain.py --append --retrain
python scripts/backfill_predictions.py
```

---

## 9. Web 应用架构

### 后端 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/forecast` | GET | 预测、历史数据、天气、风险 |
| `/api/weather` | GET | Open-Meteo 天气代理（解决国内访问限制） |
| `/api/metrics` | GET | 指定模型的 metrics.json |
| `/api/scheduler/status` | GET | 定时任务状态 |

### Dashboard v3

访问：`http://localhost:5000/dashboard/v3`

- Apple 风格三栏布局（ECharts + CSS Grid）
- 四条预测曲线（XGBoost / GRU / Transformer / 集成），可点击图例单独显示/隐藏
- 旺淡季分段阈值线（旺季红色32,800 / 淡季橙色18,400，各自只在对应区间显示）
- 顶部天气预报横向滚动条（未来 3 天，后端代理获取）
- 右侧面板：天气卡片 + 适游性风险温度计 + 推荐出行窗口（含影响因素标签）
- 数据滞后区（灰色）+ 未来预测区（蓝色）标注
- 节假日区域高亮 / 中英文切换 / 深色模式

---

## 10. 当前进度与后续计划

### 已完成

| 项目 | 状态 |
|------|------|
| 三模型实现（XGBoost/GRU/Transformer） | ✅ |
| 近三年数据过滤（2023-06-01+，规避异常期） | ✅ |
| 旺淡季样本加权训练 | ✅ |
| 单步训练 + 3步滚动推理 | ✅ |
| 节假日距离特征（days_to/since_next_holiday） | ✅ |
| 特征精简（删 rolling_mean_14 / lag_28） | ✅ |
| 集成预测（XGB×0.70 + TR×0.20 + GRU×0.10，MAE 2,186） | ✅ |
| NRMSE 归一化指标 | ✅ |
| 消融实验（GRU，6配置） | ✅ |
| 客流主导预警（超阈值即预警，天气仅辅助） | ✅ |
| 旺淡季分段阈值线（各自只在对应区间显示） | ✅ |
| 自动化数据管道（APScheduler） | ✅ |
| Dashboard v3（图例切换、天气代理、在线推理、影响因素标签） | ✅ |

### 待完成（答辩前）

| 项目 | 优先级 |
|------|--------|
| 论文 Ch.3 系统设计（架构图、数据流图） | P0 |
| 论文 Ch.4 实验结果与对比分析 | P0 |
| 论文 Ch.5 结论与 Future Work | P1 |

### Future Work（论文建议方向）

- **历史数据拼接**：将 2016-2018 正常运营期数据量级归一化后拼入训练集，增加样本多样性
- **预售票特征**：已发现九寨沟预售订票 API，积累 ≥6 个月后可作为输入特征（对节假日阶跃峰值预警有显著帮助）
- **小时级预测**：当前日级；若官网支持小时级爬取可大幅提升运营决策粒度
- **Stacking 集成**：用三模型预测值训练 meta-learner，比加权平均更优，但需额外训练集

---

## 11. 项目维护信息

**技术栈**：Python 3.11, TensorFlow 2.15, XGBoost, Flask 3.0, ECharts 5, APScheduler

```bash
pip install -r requirements_all.txt
```

**最后更新**：2026年4月20日

### 更新日志

| 日期 | 内容 |
|------|------|
| 2026-04-20 | 节假日距离特征（days_to/since_next_holiday）；删冗余特征；重训三模型；集成预测 MAE 2,186；旺淡季分段阈值线；推荐窗口影响因素标签 |
| 2026-04-20 | 预警逻辑改为客流主导；Dashboard 图例切换；天气后端代理；在线推理修复（自动检测模型输入形状） |
| 2026-04-18 | 精简至三模型（XGBoost/GRU/Transformer）；LSTM/Seq2Seq归档；预测窗口改为3天滚动推理 |
| 2026-04-12 | 气象预警阈值改为旅游舒适度绝对标准；修复前端bug |
| 2026-04-09 | 修复爬虫；预警阈值改为季节性官方承载量×80%；发现预售票API |
| 2026-04-06 | 完成消融实验、Seq2Seq注意力热力图 |
| 2026-04-03 | Dashboard v3 发布，完成初版训练 |
